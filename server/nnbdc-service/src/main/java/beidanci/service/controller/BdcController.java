package beidanci.service.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class BdcController {
    // 所有方法已被删除，因为前端不使用BdcController的任何接口
    // 背单词相关功能已在前端本地化实现
}
