org.gradle.jvmargs=-Xmx6144M -XX:+UseG1GC -XX:MaxMetaspaceSize=1g -XX:+HeapDumpOnOutOfMemoryError
org.gradle.workers.max=8
android.useAndroidX=true
android.enableJetifier=false
# 当本机装了 SDK 36 时，项目会自动使用 compileSdk=36；AGP 8.7.0 仅测试到 35，会给出提示。
# 这里按官方建议抑制该提示（不影响实际编译）。
android.suppressUnsupportedCompileSdk=35,36
android.nonTransitiveRClass=true
android.nonFinalResIds=true
org.gradle.parallel=true
org.gradle.daemon=true
org.gradle.configureondemand=true
# 配置缓存：AGP 8.7.0 已支持，但可能仍有兼容性问题，先保持禁用
# 如果遇到配置缓存错误，可以尝试启用并设置为警告模式
# org.gradle.configuration-cache=true
# org.gradle.configuration-cache.problems=warn
org.gradle.configuration-cache=false
org.gradle.caching=true
# 启用文件系统监控以加速增量构建
org.gradle.vfs.watch=true
# 优化依赖解析性能
org.gradle.dependency.verification=off
# 启用构建缓存（即使配置缓存不可用，构建缓存仍可大幅提升速度）
org.gradle.buildcache.enabled=true
#systemProp.gradle.wrapperUrl=https://mirrors.aliyun.com/gradle/
#systemProp.http.proxyHost=127.0.0.1
#systemProp.http.proxyPort=1080
#systemProp.https.proxyHost=127.0.0.1
#systemProp.https.proxyPort=1080
systemProp.org.gradle.internal.http.socketTimeout=120000
# 增加连接超时时间以解决网络问题
systemProp.org.gradle.internal.http.connectionTimeout=120000
# systemProp.javax.net.ssl.trustStoreType=PKCS12
# systemProp.javax.net.ssl.trustStorePassword=changeit
# systemProp.https.protocols=TLSv1.2,TLSv1.3
# 有些环境里如果把 trustStore 设为空，会导致 JVM 无法使用默认 cacerts，从而出现 TLS 握手失败
#（例如下载 dl.google.com / maven 仓库依赖时报 “Remote host terminated the handshake”）
# 如确需自定义证书，请把下面两项改为真实路径/密码，而不是留空：
# systemProp.javax.net.ssl.trustStore=/path/to/custom/cacerts
# systemProp.javax.net.ssl.trustStorePassword=changeit
# 额外指定客户端 TLS 协议，避免不同 JDK/Gradle 组合出现协商问题
# systemProp.jdk.tls.client.protocols=TLSv1.2,TLSv1.3

# Kotlin incremental build optimizations
kotlin.incremental=true
kotlin.incremental.java=true
# kotlin.incremental.useClasspathSnapshot=true  # 已弃用，移除
kotlin.caching.enabled=true
# 使用 daemon 模式以获得更好的性能（需要更多内存但更快）
kotlin.compiler.execution.strategy=daemon

# 设置默认的编译SDK版本
android.defaults.compileSdkVersion=36

flutter.compileSdkVersion=36
